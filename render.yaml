services:
  # A Docker web service
  - type: web
    name: backend
    runtime: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r app/requirements/requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT